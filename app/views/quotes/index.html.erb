<%# gray-50 %>
<%# gray-200 %>

<div class="flex flex-col w-full" data-controller="quotes">
  <%= turbo_frame_tag 'new_quote', class:'w-full flex flex-col items-center justify-center gap-4 my-4', id: 'new_quote' %>

  <%= render partial: 'filters' %>

  <%= render partial: 'quotes' %>
</div>