<% if filters_applied? %>
  <%= turbo_stream.replace "quotes", render('quotes') %>
<% else %>
  <%= turbo_stream.prepend "quotes", @quote %>
<% end %>

<%= turbo_stream.update "new_quote", '' %>
<%= turbo_stream.replace "filters_stats", render('filters_stats') %>
<%= render_flash %>

